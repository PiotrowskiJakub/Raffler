== Raffler

Sample application uses AngularJS and Ruby on Rails.

Ruby version = 2.3.0

Rails version = 4.2.5.1


Run application:
* get to the app directory
* bundle install
* rake db:migrate
* rake db:seed
* rails server
